<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* General form styling */
        body {
            background-color: #f8f9fa; /* Light gray background for the entire page */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        form {
            background: #ffffff; /* White background for the form */
            padding: 30px 40px;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px; /* Maximum width for the form */
        }

        /* Input group styling */
        .input-group {
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }

        .input-group-text {
            background-color: #dc3545; /* Red background for the label */
            color: #fff; /* White text */
            padding: 0.5rem 1rem;
            border: none;
            border-top-left-radius: 4px;
            border-bottom-left-radius: 4px;
            display: flex;
            align-items: center;
        }

        .form-control {
            height: calc(2.5rem + 2px);
            font-size: 1rem;
            padding: 0.5rem;
            border: 1px solid #ced4da;
            border-left: none;
            border-top-right-radius: 4px;
            border-bottom-right-radius: 4px;
            flex-grow: 1;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .form-control:focus {
            border-color: #80bdff;
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
            outline: none;
        }

        .form-control::placeholder {
            color: #aaa;
            font-style: italic;
        }

        /* Button styling */
        .btn {
            padding: 0.5rem 1rem;
            font-size: 1rem;
            border-radius: 4px;
            text-decoration: none;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.2s ease;
            border: none;
            display: inline-block;
            color: #fff;
        }

        .btn-success {
            background-color: rgb(65, 65, 185); /* Green background */
            margin-right: 10px;
        }

        .btn-success:hover {
            background-color: rgb(65, 65, 185); /* Green background */
            transform: translateY(-2px); /* Slight lift on hover */
        }

        .btn-info {
            background-color: rgb(65, 65, 185); /* Green background */
            float: right;
        }

        .btn-info:hover {
            background-color: rgb(70, 70, 170); /* Green background */
            transform: translateY(-2px); /* Slight lift on hover */
        }

        /* Additional styles for form elements */
        .border-top {
            border-top: 1px solid #6c757d;
            margin-top: 20px;
            padding-top: 20px;
        }

        .text-white {
            color: #fff;
        }

        .text-center {
            text-align: center;
        }

        /* Responsive design */
        @media (max-width: 576px) {
            form {
                padding: 20px;
            }

            .btn-success,
            .btn-info {
                display: block;
                width: 100%;
                margin-bottom: 10px;
            }

            .btn-info {
                float: none;
            }
        }
    </style>
</head>
<body>
    <form action="/forgotpass" method="post">
        <!-- email -->
        <div class="input-group">
            <span class="input-group-text" id="basic-addon1">Email</span> &nbsp;&nbsp;&nbsp;&nbsp;
            <input
                type="text"
                placeholder="Email Address"
                aria-label="Username"
                aria-describedby="basic-addon1"
                name="email"
            />
        </div>
        <!-- Buttons section -->
        <div class="border-top">
            <a class="btn btn-success" href="/logout" id="to-login">Back To Login</a>
            <button class="btn btn-info" type="submit">Recover</button>
        </div>
    </form>
</body>
</html>
