<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
   
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <style>

body {
            background-image: linear-gradient(to right, #55d7e5, #8f2aea);
        }

        .container {
            width: 30%;
            height: 600px;
            margin-top: 60px;
            border-radius: 10px;
            background-color: white;
            margin-left: 35%;
            display: flex;
            justify-content: space-around;
        }

        h1 {
            text-align: center;
            padding-top: 15px;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        }

        input {
            width: 400px;
            height: 40px;
            padding-left: 15px;
            border-radius: 8px;
            border: none;
            border: 2px solid #Efefef;
            margin-top: 30px;
            /* margin-left: 1px; */
        }

        button {
            width: 100%;
            padding: 10px 20px;
            font-size: 20px;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            font-weight: bold;
            background-color: rgb(85, 180, 85);
            color: white;
            border: 0;
            border-radius: 10px;
            margin-top: 40px;
        }


       
    </style>
</head>
<body>
    <div class="container">
       
        <div class="form-container">
            <h1>Edit Book</h1>
            <form action="/updateData?id=<%=editbook.id%>" method="post">
                <div class="form-group">
                    <input type="text" name="title" required placeholder="Enter book name" value="<%=editbook.title%>">
                </div>
                <div>
                    <input type="text" name="author" required placeholder="Enter author name" value="<%=editbook.author%>">
                </div>
                <div>
                    <input type="number" name="year" required placeholder="Enter published year" value="<%=editbook.year%>">
                </div>
                <div class="form-group">
                    <input type="number" name="price" required placeholder="Enter price" value="<%=editbook.price%>">
                </div>
                <div class="form-group">
                    <input type="number" name="copy" required placeholder="Enter published copies" value="<%=editbook.copy%>">
                </div>
                <button type="submit">Update Book</button>
            </form>
        </div>
    </div>

    
    
</body>
</html>